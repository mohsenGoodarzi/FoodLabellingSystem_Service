@model IEnumerable<FoodLabellingSystem_Service.Models.DishType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DishTypeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Member)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DishTypeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Member)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { dishTypeId = item.DishTypeId }) |
                @Html.ActionLink("Details", "Details", new { dishTypeId = item.DishTypeId  }) |
                @Html.ActionLink("Delete", "Delete", new {dishTypeId = item.DishTypeId  })
            </td>
        </tr>
}
    </tbody>
</table>
